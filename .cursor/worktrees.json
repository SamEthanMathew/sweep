{
  "setup-worktree": [
    "node -v",
    "npm -v",
    "npm ci"
  ],
  "commands": {
    "dev": "npm run dev",
    "build": "npm run build",
    "test": "npm test",
    "lint": "npm run lint"
  }
}
